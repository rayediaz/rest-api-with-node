module.exports = {
  db: {
    host: 'localhost',  // host: the host to connect to (default localhost)
    port: 28015,        // port: the port to connect on (default 28015)
    db: 'test'          // db: the default database (default test)
  }
}
